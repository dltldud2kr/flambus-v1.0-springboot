<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="flambus.app.mapper.ReviewMapper">

    <!-- 해당 가게에서 제일 많은 태그 정보 찾아오기. -->
    <select id="findReviewTag" resultType="flambus.app.dto.review.ReviewResponseDTO$ReviewTag">
<!--        SELECT rt.*, rtt.idx AS review_tag_type_idx-->
<!--        FROM review_tag AS rt-->
<!--        INNER JOIN review_tag_type AS rtt ON rt.tag_idx = rtt.tag_idx-->
<!--        INNER JOIN review AS r ON rt.review_idx = r.idx;  testest-->
    </select>

</mapper>
